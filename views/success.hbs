<section class="section-full-size">
    <div class="message-box success-box" style="margin: 0 auto;">
        <h2> Success </h2>
        {{#if message}}
        <p> {{message}} </p>
        {{/if}}
    </div>
</section>